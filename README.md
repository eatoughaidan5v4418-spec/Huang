# EasyBill 极简记账 App

一个简单易用的个人记账应用，帮助你轻松管理每日支出。

## ✨ 功能特性

- **快速记账**：一键记录支出，支持多种分类
- **直观统计**：饼图展示月度支出分布
- **数据持久化**：本地存储，隐私安全
- **极简设计**：界面清爽，操作简单

## 🚀 快速开始

### 环境要求

- Node.js 18+
- npm 或 yarn
- iOS 模拟器 / Android 模拟器 / 真机

### 安装依赖

```bash
cd EasyBill
npm install
```

### 启动项目

```bash
npx expo start
```

### 运行平台

- **iOS**: 按 `i` 键或运行 `npm run ios`
- **Android**: 按 `a` 键或运行 `npm run android`
- **Web**: 按 `w` 键或运行 `npm run web`

## 📁 项目结构

```
EasyBill/
├── app/                    # 页面路由
│   ├── _layout.tsx        # 应用布局（含底部导航）
│   ├── index.tsx          # 首页（总支出 + 最近记录）
│   ├── add-expense.tsx    # 记账页面（模态窗口）
│   └── statistics.tsx    # 统计页面（饼图展示）
├── components/            # 可复用组件
│   └── ui/                # 基础 UI 组件
│       └── CategoryIcon.tsx  # 分类图标组件
├── constants/            # 常量配置
│   ├── categories.ts     # 分类配置
│   └── theme.ts          # NativeBase 主题
├── store/                # 状态管理
│   └── useExpenseStore.ts  # Zustand store
├── types/                # TypeScript 类型定义
│   └── index.ts          # 类型导出
├── utils/                # 工具函数
│   └── storage.ts        # AsyncStorage 操作
├── App.tsx               # 应用入口
├── package.json          # 依赖配置
├── tsconfig.json         # TypeScript 配置
├── babel.config.js       # Babel 配置
└── metro.config.js      # Metro 配置
```

## 🛠 技术栈

| 技术 | 用途 |
|------|------|
| React Native | 移动端跨平台框架 |
| Expo | 开发工具和运行时 |
| TypeScript | 类型安全 |
| NativeBase | UI 组件库 |
| Zustand | 状态管理 |
| AsyncStorage | 本地数据持久化 |
| Lucide React Native | 图标库 |
| React Native Chart Kit | 图表展示 |

## 📱 核心功能

### 首页
- 显示当月总支出金额
- 展示最近 5 条记账记录
- 快速添加记账入口

### 记账
- 金额输入（支持小数）
- 5 种分类选择（餐饮、交通、购物、娱乐、其他）
- 备注功能
- 模态窗口交互

### 统计
- 月份切换查看
- 饼图展示各分类占比
- 分类明细列表

## 🎨 分类配置

| 分类 | 图标 | 颜色 |
|------|------|------|
| 餐饮 | Utensils | #FF6B6B |
| 交通 | Bus | #4ECDC4 |
| 购物 | ShoppingBag | #45B7D1 |
| 娱乐 | Film | #96CEB4 |
| 其他 | MoreHorizontal | #DDA0DD |

## 📝 开发指南

### 添加新分类

1. 在 `constants/categories.ts` 中添加分类配置
2. 在 `types/index.ts` 中更新 CategoryType 类型
3. 在 `components/ui/CategoryIcon.tsx` 中添加图标映射

### 修改主题

在 `constants/theme.ts` 中修改 NativeBase 主题配置。

### 状态管理

所有状态逻辑位于 `store/useExpenseStore.ts`，使用 Zustand 实现：
- 自动持久化到 AsyncStorage
- 计算属性（当月统计、分类占比等）

## 📄 许可证

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！
